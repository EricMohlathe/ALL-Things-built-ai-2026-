{
  "name": "@cmw/core",
  "version": "1.0.0",
  "private": true,
  "description": "Domain logic — entities, framework definitions, scoring, checklist rules, coherence math. Pure TS, no I/O.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^3.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
